{
  "name": "query-quest",
  "private": true,
  "scripts": {
    "postinstall": "cd src && npm install --legacy-peer-deps",
    "build": "cd src && npx prisma generate && npm run build",
    "start": "cd src && npm start"
  },
  "engines": {
    "node": "20.x"
  }
}
